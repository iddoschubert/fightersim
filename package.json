{
  "name": "fighter-sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "multiplayer": "npm run build && node server/server.js"
  },
  "dependencies": {
    "three": "^0.168.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/three": "^0.168.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
